[project]
name = "careerpilot-openai-agents"
version = "0.1.0"
description = "Bounded fake-first OpenAI Agents SDK interview laboratory."
requires-python = ">=3.13,<3.14"
dependencies = [
  "fastapi>=0.135,<0.136",
  "openai-agents>=0.8,<0.9",
  "pydantic-settings>=2.10,<3",
]

[build-system]
requires = ["uv_build>=0.11.29,<0.12"]
build-backend = "uv_build"
