[project]
name = "careerpilot-core"
version = "0.1.0"
description = "Framework-independent CareerPilot AI core package shell."
requires-python = ">=3.13,<3.14"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.29,<0.12"]
build-backend = "uv_build"
