# Phase 16 Exercise Answers

1. Step-up strengthens identity recency; approval binds the exact consequential proposal. Neither
   alone proves both properties. The local values are reference contracts, not production proof.
2. `::1` is not global, so `validate_outbound_destination` raises `non_public_address`.
3. Otherwise a valid older backup can recreate data whose primary record was later deleted.
4. Add a category/control/payload/expected entry and ensure the existing benign item stays allowed.
5. SAST checks source patterns; SCA dependencies; DAST running HTTP behavior; secrets credentials;
   licenses use obligations; container scans image packages/config; IaC scans intended resources.
6. Examples: identity verification/lawful basis, final retention/legal holds, and breach notification
   thresholds/timelines. Provider transfers and employment-AI duties also require review.
