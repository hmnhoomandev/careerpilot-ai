# Phase 5 Exercise Answers

1. Post-filtering permits unauthorized rows to enter candidate memory, telemetry, or
   caches. In-query predicates keep them behind the data boundary.
2. It tells downstream code that text has no instruction authority. It does not prove
   the content is true, harmless, complete, or free of undiscovered attacks.
3. Reproducibility and compatibility: a changed component can trigger complete,
   auditable reindexing rather than comparing incompatible derivatives.
4. Recall asks whether relevant evidence was found; precision measures returned noise;
   MRR rewards an early first relevant result; grounding checks passage support; citation
   correctness checks provenance points to the stored source.
5. It recognizes only deterministic development signals and lacks updated signatures,
   behavioral analysis, isolation, operational monitoring, and a vendor threat feed.
6. Raw bytes, normalized chunks, embeddings, lexical indexes, caches, and replicas.
   Backups require a separately documented recovery/deletion policy.
