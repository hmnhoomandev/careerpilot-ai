# Phase 6 Exercises

1. Why must a tool call pass both `tool.invoke` and a capability-specific permission?
2. Why is an idempotency key scoped to tenant, actor, tool, and input fingerprint?
3. What is the difference between retry and idempotent replay?
4. Why is `approval.request` high-risk metadata but not itself approval-gated?
5. Why are audit lookup and approval request absent from MCP discovery?
6. How does an MCP tool differ from an A2A agent?
