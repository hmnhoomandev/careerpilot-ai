# Phase 14 exercise answers

1. Tab reveals the skip link; Enter moves focus to main content, bypassing repeated navigation.
2. Validation returns null and the renderer shows a blocked-content alert with no action.
3. Browser code is attacker-controlled; FastAPI must rederive identity, tenant and permission.
4. Sidebar becomes a horizontal navigation row, grids become one column, and all content and
   controls remain in document order.
5. The UI gives a retry-oriented local API message and preserves the page. It does not expose
   stack traces, network internals or secrets.
