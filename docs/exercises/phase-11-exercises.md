# Phase 11 exercises

1. Why does an Agent Card not authorize delegation?
2. What happens when the same task ID is submitted with changed input?
3. Why is a foreign tenant shown not-found instead of forbidden?
4. Disable an adapter and describe why the registry must not choose another runtime.
5. List the controls still required before exposing public A2A JSON-RPC endpoints.
