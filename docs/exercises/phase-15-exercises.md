# Phase 15 exercises

1. Explain why `prompt_version` is safe metadata but prompt text is not.
2. Request an unavailable paid route and identify why selecting the fake route would be fallback.
3. Compare budget estimation, reservation and reconciliation.
4. Explain why `NO_CONTENT` does not disable ADK prompt-response GCS upload by itself.
5. Add a deterministic evaluation metric and make its threshold fail without lowering the bar.
