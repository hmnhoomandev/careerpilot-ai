# Phase 4 Exercises

1. Explain why `WHERE tenant_id = ?` is still needed when IDs are UUID-like.
2. Two clients load profile version 4. Client A saves first. What should Client B see?
3. Why does a `.pdf` extension not prove that a file is a safe PDF?
4. In the PostgreSQL test, a duplicate skill fails after the version update SQL.
   Why does the stored version remain unchanged?
5. Identify two reasons SQLite would be misleading evidence for this adapter.
