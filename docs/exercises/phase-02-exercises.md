# Phase 2 Exercises

1. Draw the dependency direction from the UI to the core and explain why core
   owns the repository protocol.
2. Predict the shared terms for profile text “Python accessibility engineer” and
   job text “Accessibility engineer using Python and Go.” Then verify with a unit
   test.
3. Find the maximum lengths for all three user inputs. Explain why browser limits
   cannot replace API validation.
4. Trace a correlation ID from response header to JSON result and structured log.
5. Restart the API and explain why a previous profile ID returns 404.
6. Identify every field deliberately excluded from logs and spans.
7. Change a response field name locally and observe which contract/UI test fails.
8. Explain why this result must not be described as candidate-to-job fit.
