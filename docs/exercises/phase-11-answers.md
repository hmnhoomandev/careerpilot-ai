# Phase 11 exercise answers

1. It is untrusted capability metadata; caller and resource policy must be evaluated by
   the receiving service.
2. The fingerprint differs and the request fails with an explicit conflict.
3. Non-enumerating behavior avoids confirming another tenant's task exists.
4. The task fails as unavailable; selecting another runtime would be silent fallback and
   could change data handling, quality, cost, and consent assumptions.
5. Official servers/clients, workload identity, card trust and revocation, durable stores,
   rate limits, payload redaction, retention/deletion, tracing, and security/legal review.
