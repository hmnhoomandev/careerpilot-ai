# Capstone assessment answers

1. PostgreSQL owns business/profile/evidence/draft/approval records; pgvector indexes evidence; LangGraph owns bounded analysis; Temporal owns durable wait/process; audit records decisions.
2. Treat document text as untrusted, flag injection, retrieve only authorized chunks and block/label the unsupported Kubernetes claim as requiring confirmation.
3. LangGraph orchestrates bounded graphs; Temporal durable processes; ADK/OpenAI bounded specialists; MCP tools; A2A remote capability discovery/tasks.
4. OIDC authenticates the coach; membership/RBAC grants candidate permission; ABAC checks delegation, purpose, sensitivity and state; every query filters tenant/resource scope.
5. Retry re-executes; the stable key prevents duplicate effect; replay rebuilds deterministic history; recovery resumes; compensation semantically reverses when possible; fallback changes provider/model and is prohibited silently.
6. Fail visibly with the selected provider identity/reason. Do not switch because privacy, quality, cost and consent assumptions differ.
7. Authorize and approve, enter recoverable deletion, tombstone source and derivatives, propagate to indexes/caches/replicas, preserve minimized justified audit, and apply tombstones on restore. Lawful basis, retention, exceptions and workflow/history erasure require legal review.
8. At most 0.5% eligible request failure; exclude only documented user/policy outcomes. Exhaustion freezes risky releases and prioritizes reliability. Production telemetry—not local tests—is required.
9. Scope is part of each gate. Local in-process evidence cannot measure managed identity, network, storage, traffic, alerts, cost, restore or operations.
10. Use approved isolated resources, representative synthetic/consented shape, expected/2×/saturation load, 24-hour soak, dependency failures, PITR restore, tenant/adversarial/accessibility checks, cost alerts and rehearsed digest rollback.
11. SBOM inventories; SCA matches known advisories; provenance describes build/materials; signature binds identity to digest. Unknown flaws, misconfiguration, compromised builders and runtime behavior remain.
12. Examples: production identity/tenant proof, managed restore/RPO/RTO, representative capacity/SLOs, provider privacy/cost/region, signed supply chain, on-call readiness, legal retention and device accessibility.
