# Web Application

The Next.js/React/TypeScript application owns browser presentation. Phase 3 wraps the
deterministic comparison in synthetic local login, server-validated tenant/ role
context, owner-only role management, and an authorized audit viewer. Tokens remain only
in component memory. It does not call a model or persist browser data.
