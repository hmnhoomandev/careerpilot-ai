# Web Application

The Next.js/React/TypeScript application owns browser presentation. Phase 4 adds
versioned profile editing and evidence-metadata registration to the synthetic,
tenant-authorized journey. Tokens remain only in component memory. Selected file bytes
are not uploaded; only allowlisted metadata is sent and displayed as quarantined.
